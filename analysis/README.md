# Analysis Workspace

This directory is for Phase A analysis code, notebooks, and reproducibility scripts.

Required controls:

- Implement only analyses defined in the locked protocol unless explicitly labeled exploratory.
- Pin random seeds and software versions.
- Keep estimation precision simulation scripts reproducible and immutable once used for a locked execution decision.
- Store generated tables or figures in `results/` or `dataset/` as appropriate.
